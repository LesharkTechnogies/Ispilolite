package models

// Job and installation models are defined here as the platform's job flow is
// built out (see TODO.md). This file currently only declares the package so the
// models package compiles for the search subsystem, which depends on
// ISP/Technician/Location in this package but not (yet) on Job.
