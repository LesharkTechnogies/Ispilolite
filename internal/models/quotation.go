package models

// Quotation models are defined here as the platform's quotation flow is built
// out (see TODO.md). This file currently only declares the package so the
// models package compiles for the search subsystem.
